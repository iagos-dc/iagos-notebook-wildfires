# AERIS SSO credentials (Jasypt-encrypted)
# Run: python src/fr/aeris/auth/generate_credentials.py to generate these values
AERIS_ENCRYPTED_EMAIL=""
AERIS_ENCRYPTED_PASSWORD=""

# Master password for decryption (optional — you can also pass it at runtime or be prompted)
# AERIS_MASTER_PASSWORD=""

# Keycloak server config (defaults are set in authentication.py — override only if needed)
# AERIS_KC_SERVER_URL="https://sso.aeris-data.fr/auth/"
# AERIS_KC_REALM="aeris"
# AERIS_KC_CLIENT_ID="aeris-public"
